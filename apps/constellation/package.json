{
  "name": "@spicetify-apps/constellation",
  "version": "1.2.0",
  "private": true,
  "main": "dist/index.js",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "download": "curl -fsSL https://raw.githubusercontent.com/Prog-Jacob/spicetify-apps/main/install.sh | bash -s constellation",
    "symlink": "DEST=\"$(dirname $(spicetify -c))/CustomApps/constellation\" && rm -rf \"$DEST\" && ln -sfn \"$PWD/dist\" \"$DEST\" && spicetify config custom_apps constellation >/dev/null"
  }
}
